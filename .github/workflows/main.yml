name: Build and Release

on:
  push:
    branches: [ main ]
    paths:
      - 'package.json'
    tags:
      - 'v*'
  pull_request:
    branches: [ main ]

permissions:
  contents: write

jobs:
  build:
    # Skip if the commit was made by the GitHub Actions bot
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 0
        token: ${{ secrets.PAT_TOKEN }}
    
    - name: Use Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18.x'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Update manifest and create tag
      env:
        GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
      run: |
        # Skip if commit message indicates this is an automated version update
        if [[ "${{ github.event.head_commit.message }}" == *"skip ci"* ]]; then
          echo "Skipping version update as this is an automated commit"
          exit 0
        fi
        
        VERSION=$(node -p "require('./package.json').version")
        echo "Updating manifest.json version to $VERSION"
        sed -i "s/\"version\": \".*\"/\"version\": \"$VERSION\"/" public/manifest.json
        
        # Configure git
        git config --global user.name 'GitHub Actions'
        git config --global user.email 'actions@github.com'
        
        # Add and commit manifest changes
        git add public/manifest.json
        git commit -m "chore: Update manifest version to $VERSION [skip ci]" || echo "No manifest changes"
        
        # Check if tag exists and delete it if it does
        if git rev-parse "v$VERSION" >/dev/null 2>&1; then
          echo "Tag v$VERSION exists, deleting and recreating"
          git tag -d "v$VERSION"
          git push origin ":refs/tags/v$VERSION" || true
        fi
        
        # Create and push new tag
        git tag -a "v$VERSION" -m "Release version $VERSION"
        git push origin "v$VERSION"
        
        # Push manifest changes
        git push origin HEAD:${{ github.ref }}

    - name: Build
      run: npm run build
      env:
        NODE_ENV: production

    - name: Get Version
      id: get_version
      run: echo "VERSION=$(node -p "require('./package.json').version")" >> $GITHUB_ENV

    - name: Create artifacts directory
      run: mkdir -p artifacts

    - name: Create versioned zip
      run: |
        cd dist
        zip -r ../artifacts/azure-devops-wiki-editor-v${VERSION}.zip *

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: extension-dist-v${VERSION}
        path: artifacts/azure-devops-wiki-editor-v${VERSION}.zip
        if-no-files-found: error

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
    - name: Get Version
      id: get_version
      run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_ENV

    - name: Download artifacts
      uses: actions/download-artifact@v4
      with:
        name: extension-dist-v${VERSION}
        path: artifacts

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: artifacts/azure-devops-wiki-editor-v${VERSION}.zip
        generate_release_notes: true
        tag_name: ${{ github.ref_name }}
      env:
        GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
